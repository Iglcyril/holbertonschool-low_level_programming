#include "main.h"
/**
 * print_line - Entry point
 *@n : number of time character _ repeat
 * Return : Always : 0 (Success)
 */
void print_line(int n)
{
	int i, j;

	if (n <= 0)
	{
		for (i = 0; i < n; i++)
			_putchar(' ');
		for (j )


		_putchar('\n');

		for (i = 0; i < n; i++)
		{
		_putchar ('_');
		}
		_putchar ('\n');
	}
}
